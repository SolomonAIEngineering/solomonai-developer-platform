import { createAppRoute } from "@trigger.dev/nextjs";
import { client } from "@v1/jobs";

export const runtime = "nodejs";
export const maxDuration = 300; // 5min

import "@v1/jobs";

export const { POST, dynamic } = createAppRoute(client);
